# mask = (torch.rand(X.shape) > dropout).float()

# mask = (torch.rand(X.shape) > dropout).float()

# mask = (torch.rand(X.shape) > dropout).float()

# mask = (torch.rand(X.shape) > dropout).float()

# mask = (torch.rand(X.shape) > dropout).float()

print("!23")


print("!23")

print("!23")

print("!23")

